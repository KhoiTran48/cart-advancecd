var init={
    by:"",
    value:""
}

const SortReducer=(state=init, action)=>{
    switch (action.type) {
        case value:

            break;
    
        default:
            return {...state};
    }
}

export default SortReducer;