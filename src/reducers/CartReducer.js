var init=[

]

const CartReducer=(state=init, action)=>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return [...state];
            break;
    }
}